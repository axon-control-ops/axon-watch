#!/usr/bin/env bash
# PATH wrapper → scripts/ops/axon-agent-terminal-job.sh
set -euo pipefail
repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "${repo_root}/scripts/ops/axon-agent-terminal-job.sh" "$@"
