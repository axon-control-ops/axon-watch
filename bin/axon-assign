#!/usr/bin/env bash
# PATH wrapper → scripts/ops/axon-assign.sh
set -euo pipefail
SCRIPT_PATH="$(readlink -f -- "${BASH_SOURCE[0]}")"
repo_root="$(cd "$(dirname "${SCRIPT_PATH}")/.." && pwd)"
exec "${repo_root}/scripts/ops/axon-assign.sh" "$@"
