/** Compact activity lines for the Team panel persona dock (live shift preview). */

export {
  buildSidebarAgentTranscriptLines as buildTeamPanelTranscriptLines,
  type SidebarTranscriptLine as TeamPanelTranscriptLine,
  type SidebarTranscriptLineKind as TeamPanelTranscriptLineKind,
} from './sidebar-agent-transcript-view';
