[project]
name = "axon-watch"
version = "0.1.0"
description = "Axon-Watch bootstrap, verification, and governance scaffold."
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.unittest]
start-directory = "tests"
